---
name: rust-unsafe-patterns
description: "Use Rust unsafe code correctly with raw pointers, FFI boundaries, and safety invariants"
---

# Rust Unsafe Patterns

Use Rust unsafe code correctly with raw pointers, FFI boundaries, and safety invariants
